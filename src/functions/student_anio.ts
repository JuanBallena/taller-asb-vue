import { PARAMETER_ACTIVE } from '@/definitions/parameter_definition';
import { StudentYear } from '@/interfaces/StudentYear';

const SUSPENDED = "SUSPENDIDO"

// export function getStatusText(studentAnio: StudentAnio): string {

//   return studentAnio.suspended ? SUSPENDED : studentAnio.status.description
// }

// export function getStatusColor(studentAnio: StudentAnio): string {

//   if (studentAnio.suspended) {
//     return 'secondaryColor';
//   }
//   return studentAnio.status.id == PARAMETER_ACTIVE ? 'successColor' : 'dangerColor';
// }